package com.alura.forohub.challengeforohub.domain.topico.validations.create;

import com.alura.forohub.challengeforohub.domain.topico.dto.CrearTopicoDTO;

public interface ValidarTopicoCreado {

    void validate(CrearTopicoDTO data);
}
